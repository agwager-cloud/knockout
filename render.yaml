services:
  - type: web
    name: knockout-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm run server:start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 22
